function latinLetters() {
    for (let i = 97; i <= 122; i++) { // виж ASCII таблицата
        console.log(String.fromCharCode(i)); // returns a string created by using the specified sequence of Unicode values.
    }
}
latinLetters();
